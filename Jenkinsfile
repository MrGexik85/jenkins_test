pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo Hello world!'
            }
        }
        stage('deploy') {
            steps {
                sh 'echo Deploy'
            }
        }
    }
}